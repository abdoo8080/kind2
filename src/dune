(executable
  (public_name kind2)
  (libraries num threads yojson str)
  (modules_without_implementation messaging SolverSig testgen testgenLib))
(include_subdirs unqualified)

(env
  (dev
    (flags (:standard -warn-error -A))))
